version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports: ["8000:8000"]
  app:
    build:
      context: .
      dockerfile: Dockerfile.app
    ports: ["5173:80"]
    depends_on: [api]
